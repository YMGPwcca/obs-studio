name: Task 11 one-shot integrator

on:
  push:
    branches:
      - engine-protocol-v2
    paths:
      - .github/workflows/task11-one-shot-integrator.yaml

permissions:
  contents: write

jobs:
  integrate:
    if: github.event.head_commit.message == 'chore(engine): run Task 11 one-shot integrator'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          fetch-depth: 0

      - name: Apply reviewed Task 11 integration
        shell: bash
        run: |
          set -euo pipefail
          python3 .github/scripts/task11-integrate.py
          git diff --check
          test ! -e engine/runtime_filter_source_duplicate_v2.cpp
          test ! -e engine/TASK11_INTEGRATION_NOTE.tmp
          test ! -e .github/TASK11_STAGING_DO_NOT_USE.tmp
          test ! -e .github/workflows/task11-one-shot-integrator.yaml
          test ! -e .github/scripts/task11-integrate.py

      - name: Commit integration result
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'Task 11 integration bot'
          git config user.email 'actions@users.noreply.github.com'
          git add -A
          git diff --cached --check
          git commit -m 'feat(engine): integrate protocol v2 filter namespace'
          git push origin HEAD:engine-protocol-v2
