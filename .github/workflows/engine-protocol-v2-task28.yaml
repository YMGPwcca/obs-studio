name: Engine Protocol v2 Task 28 Verification

on:
  push:
    branches:
      - phase3-output-stack
  workflow_dispatch:

permissions:
  contents: read

jobs:
  streaming-v1:
    name: Build and verify streaming orchestration
    runs-on: windows-2025-vs2026
    timeout-minutes: 35
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
        with:
          submodules: recursive
          fetch-depth: 0
      - name: Build Windows minimal runtime
        uses: ./.github/actions/build-obs
        with:
          target: x64
          config: RelWithDebInfo
      - name: Audit normal package
        shell: pwsh
        run: '& .github/scripts/engine-protocol-v2-task28-package-audit.ps1 -InstallRoot (Resolve-Path ''build_x64/install'')'
      - name: Build CI-only streaming fixtures
        shell: pwsh
        run: '& .github/scripts/engine-protocol-v2-task28-build-fixture.ps1'
      - name: Stage CI-only streaming fixtures
        shell: pwsh
        run: '& .github/scripts/engine-protocol-v2-task28-stage-fixture.ps1 -InstallRoot (Resolve-Path ''build_x64/install'') -FixtureDirectory (Resolve-Path ''build_x64/engine/RelWithDebInfo'')'
      - name: Run Task 28 streaming integration
        shell: pwsh
        run: '& .github/scripts/engine-protocol-v2-task28.ps1 -InstallRoot (Resolve-Path ''build_x64/install'')'
      - name: Remove CI-only streaming fixtures
        if: always()
        shell: pwsh
        run: '& .github/scripts/engine-protocol-v2-task28-remove-fixture.ps1 -InstallRoot (Resolve-Path ''build_x64/install'')'
      - name: Audit package after fixture cleanup
        if: always()
        shell: pwsh
        run: '& .github/scripts/engine-protocol-v2-task28-package-audit.ps1 -InstallRoot (Resolve-Path ''build_x64/install'')'
